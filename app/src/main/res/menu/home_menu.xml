<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_action_search_light"
        android:title="@string/button_to_search_page"
        app:showAsAction="always"
        android:showAsAction="always"/>
    <item
        android:id="@+id/action_category"
        android:icon="@drawable/ic_action_category_light"
        android:title="@string/menu_item_to_category_page"
        app:showAsAction="always"
        android:showAsAction="always"/>
</menu>
